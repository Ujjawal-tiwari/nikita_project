package com.nikita.creditrisk.config;

// OpenAI fallback removed. Groq is now the primary LLM (configured in
// application.yml).
// This class is intentionally empty. The FallbackService handles rule-based
// fallbacks.
